# Ordering Microservice

This microservice handles orders placed from the frontend by calculating costs
and then placing them in the database using the database microservice.

This microservice runs on Python Version >3.10.
To install all dependencies, run `pip install -r requirements.txt`.
You may choose to create a venv for this.